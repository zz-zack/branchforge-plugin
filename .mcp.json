{
  "mcpServers": {
    "branchforge": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/orchestrator.mjs"],
      "env": {
        "BF_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
